'use client'

import { Button } from '@/components/ui/button'

export function OrdersFilters() {
  return (
    <div className='flex items-center gap-4'>
      <Button variant='outline'>ดาวน์โหลดข้อมูล </Button>
    </div>
  )
}
